#include "rechenoperationen.h"
#include <iostream>
#include <fstream>
using namespace std;

float addieren(float x, float y)
{
    return x + y;
}
float subtrahieren(float x, float y)
{
    return x - y;
}
float dividieren(float x, float y)
{
    return x / y;
}
float multiplizieren(float x, float y)
{
    return x * y;
}